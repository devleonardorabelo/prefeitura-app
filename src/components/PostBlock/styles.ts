import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingHorizontal: 24,
    marginBottom: 16,
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
    alignSelf: 'center',
    marginBottom: 8,
  },
  text: {
    fontSize: 16,
    marginBottom: 16,
    flex: 1,
  },
  postContainer: {
    width: '100%',
    flexDirection: 'row',
  },
});
